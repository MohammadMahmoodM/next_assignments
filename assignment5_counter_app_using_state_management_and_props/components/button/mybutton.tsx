import React from 'react'

export default function MyButton(props) {
    return (
        <div>
            <button onClick={props.nextFunction}>
               Button for 
            </button>
        </div>
    )
}
